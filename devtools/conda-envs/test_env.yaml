name: test
channels:

  - conda-forge
  - pyscf
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - scipy
  - numpy
  - pyscf
 
    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

